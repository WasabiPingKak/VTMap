# Ruff — Python linter & formatter
# https://docs.astral.sh/ruff/

target-version = "py312"
line-length = 100
[lint]
# 啟用的規則集
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort (import 排序)
    "B",    # flake8-bugbear (常見 bug pattern)
    "UP",   # pyupgrade (過時語法升級)
]
ignore = [
    "E501",  # 行長度由 formatter 處理
]

[lint.per-file-ignores]
# crawler 在本機執行(Python 3.10),不能用 datetime.UTC(3.11+)
"crawler/**" = ["UP017"]

[lint.isort]
known-first-party = ["schemas", "services", "routes", "utils", "tests", "crawler"]

[format]
quote-style = "double"
indent-style = "space"
